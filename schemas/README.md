# Schemas

Machine-readable schemas will define the canonical structure for Features, Patterns, and AI selections.

Initial targets:

- `feature.schema.json`
- `pattern.schema.json`
- `selection.schema.json`

Schema design should be finalized after the conceptual model in Issue #2 is agreed, so the machine representation follows the domain model rather than prematurely constraining it.
